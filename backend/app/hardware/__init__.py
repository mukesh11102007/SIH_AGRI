"""Hardware integration package.

This package contains data source adapters for physical sensors
connected via serial/Bluetooth (e.g. HC-05).

The hardware listener is only started when HARDWARE_ENABLED=true in .env.
When disabled, this package is imported but nothing is executed.
"""
